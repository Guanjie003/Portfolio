# The site's real domain. Used for canonical URLs, the sitemap and the OG image.
# On Vercel, set this under Project Settings -> Environment Variables.
# Optional: Vercel supplies its own URL, so only set this for a custom domain.
# NEXT_PUBLIC_SITE_URL=https://portfolio-mxan.vercel.app
